module.exports = {
    name: 'info',
    description: 'information about the song currently playing, such as its title',
    instructions: 'to be implemented',
    execute(msg, args)
    {
        const { guildList } = msg.client;
    }
}